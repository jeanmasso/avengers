{# Template de base pour toutes les pages du site #}
{# Contient : header, navigation, footer et chargement de Bootstrap 5.3.3 #}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {# Favicon personnalisé - Icône 📚 (livres) #}
        <link rel="icon" type="image/svg+xml" href="{{ asset('favicon.svg') }}">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% block stylesheets %}
        {% endblock %}
    </head>
    <body>
        {# === EN-TÊTE === #}
        <header class="bg-primary text-white py-3 mb-4">
            <div class="container">
                <a href="{{ path('home') }}" class="d-flex align-items-center text-white text-decoration-none" data-turbo="false">
                    <span class="fs-2 me-2">📚</span>
                    <h1 class="h3 mb-0">Symfony dans tous ses états !</h1>
                </a>
            </div>
        </header>

        {# === NAVIGATION PRINCIPALE === #}
        {# Menu avec liens vers : Accueil, Marque-pages, Livres, Le Cailloux (Faune/Flore) #}
        <nav class="navbar navbar-expand-lg navbar-light bg-light mb-4" data-turbo="false">
            <div class="container">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('home') }}">{{ 'nav.home'|trans }}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('bookmark_index') }}">{{ 'nav.bookmarks'|trans }}</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('book_index') }}">{{ 'nav.books'|trans }}</a>
                        </li>
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                {{ 'nav.cailloux'|trans }}
                            </a>
                            <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                                <li><a class="dropdown-item" href="{{ path('faune_index') }}">{{ 'nav.faune'|trans }}</a></li>
                                <li><a class="dropdown-item" href="{{ path('flore_index') }}">{{ 'nav.flore'|trans }}</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>

        {# === CONTENU PRINCIPAL === #}
        {# Zone de contenu redéfinie par les templates enfants #}
        {% block body %}{% endblock %}

        {# === PIED DE PAGE === #}
        <footer class="bg-dark text-white text-center py-3 mt-5">
            <div class="container">
                <p class="mb-0">© {{ "now"|date("Y") }} - Créé par Jean Masso</p>
            </div>
        </footer>

        {# === SCRIPTS JAVASCRIPT === #}
        {# Chargement de Stimulus et Bootstrap JS #}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    </body>
</html>
